import CartContent from "./CartContent";

export const metadata = {
  title: "结算 — Smart Standing Desk",
  description: "填写配送信息并完成订单",
};

export default function CartPage() {
  return <CartContent />;
}
